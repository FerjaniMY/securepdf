# securepdf
Local AI PDF redaction &amp; anonymization. Strip sensitive data (PII, PHI, financial, custom entities) from PDFs on your machine before sending to cloud LLMs like ChatGPT. No GPU required.
